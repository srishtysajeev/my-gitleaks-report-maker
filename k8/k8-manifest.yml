# Save the output of this file and use kubectl create -f to import
# it into Kubernetes.
#
# Created with podman-4.9.4-rhel

# NOTE: If you generated this yaml from an unprivileged and rootless podman container on an SELinux
# enabled system, check the podman generate kube man page for steps to follow to ensure that your pod/container
# has the right permissions to access the volumes added.
---
apiVersion: batch/v1
kind: Job
metadata:
  name: gitleaks-report-job
  namespace: zsw36318
  labels:
    app: gitleaks-report
spec:
  template:
    spec:
      securityContext:
        runAsUser: 1229500
        runAsGroup: 1229500

      containers:
      - name: gitleaks-report
        image: ghcr.io/srishtysajeev/gitleaks-reports:latest

        # Run your script or entrypoint here
        command: ["bash", "-c", "./app.sh"]
        args: ["-c", "echo 'Running gitleaks report...'"]

        env:
        - name: TERM
          value: xterm
        - name: TOKEN
          valueFrom:
            secretKeyRef:
              name: mysecret
              key: TOKEN
      restartPolicy: Never



        volumeMounts:
        - name: dls-tmp-reports
          mountPath: /reports
          mountPropagation: HostToContainer

        - name: dls-tmp-data
          mountPath: /data
          mountPropagation: HostToContainer

      restartPolicy: Never

      volumes:
      - name: dls-tmp-reports
        hostPath:
          path: /dls/tmp/zsw36318/reports
          type: Directory
      - name: dls-tmp-data
        hostPath:
          path: /dls/tmp/zsw36318/data
          type: Directory
